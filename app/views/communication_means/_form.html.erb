<%= form_with(model: communication_mean) do |form| %>
  <% if communication_mean.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(communication_mean.errors.count, "error") %> prohibited this communication_mean from being saved:</h2>

      <ul>
        <% communication_mean.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
